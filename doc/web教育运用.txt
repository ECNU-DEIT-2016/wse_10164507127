pwd 
ls
cd   //直接到新建的文件夹中，既切换目录
dir
rm pub 删除命令
 web 教育应用设计语言与开发（9月29）……
=>相当于{ }
stagehand 自动生成模板工具
目录有web lib 
webdev serve
export  后接个网络地址，引入其它地址的东西
ctrl+p快捷进入
HTML 怎么呈现的看后缀为dartde 文件里面
css 展示是什么样式的
directives:[ ]依附于那个文件
加组件：在app_component,dart中的ｄｉｒｅｃｔ后，多依附一个

直接复制一个todolist 文件，更改名字，找到对应的代码（看视频）

先pub get
后 webdev serve 出来的网址，查看自己的组件是否加成功
新建一个文件doc ,get status ,发现新建的文件
最下面的md文件

markdown
命令行敲set